#include <stdio.h>

int main (void)
{
    int a = 10, b = 20;
    printf("Arithmetic Operators Demo: \n");
    printf("a + b = %i\na - b = %i\na / b = %i\na * b = %d\na %% b = %d\n", a + b, a - b, a / b, a * b, a % b);
}